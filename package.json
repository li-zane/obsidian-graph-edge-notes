{
  "name": "graph-edge-notes",
  "version": "0.3.0",
  "description": "Editable edge annotations for the Obsidian graph view.",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "dependencies": {
    "pixi.js": "^7.4.2",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "esbuild": "^0.25.1",
    "obsidian": "^1.7.2",
    "typescript": "^5.6.3"
  }
}
